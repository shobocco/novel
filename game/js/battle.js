export function battleOptions(enemy,enemies){const data=enemies[enemy]||{name:enemy,battleType:"final",bahamutConditionCost:5,bahamutTrustOnHeroFight:2};if(data.battleType==="final")return[{text:`${data.name}に全力で挑む`,result:"win"},{text:"敗北ルートを確認する（BAD END C）",result:"lose"}];return[{text:"自分で戦う",result:"win",mode:"hero"},{text:"バハムートに任せる",result:"win",mode:"bahamut"},{text:"撤退に失敗する（BAD END C）",result:"lose"}]}
export function applyBattleResult(state,enemy,enemies,option){const data=enemies[enemy]||{};if(option.result==="win"&&option.mode==="hero")state.bahamutTrust+=(data.bahamutTrustOnHeroFight||2);if(option.result==="win"&&option.mode==="bahamut"){state.bahamutCondition=Math.max(0,state.bahamutCondition-(data.bahamutConditionCost||5));state.bahamutBattleCount++}console.debug("Battle result",enemy,option)}
export const isRpgBattle=enemy=>["goblin","orc","demon_king","god"].includes(enemy);
export function createRpgBattle(enemy,enemies,state){const specs={goblin:{hp:26,damage:5,guard:2,breath:16},orc:{hp:40,damage:6,guard:2,breath:16},demon_king:{hp:60,damage:8,guard:3,breath:24},god:{hp:150,damage:12,guard:4,breath:0,alliesAuto:true}}[enemy]||{hp:30,damage:5,guard:2,breath:16};const finalBattle=["demon_king","god"].includes(enemy),heroLevel=finalBattle?Math.max(state.heroLevel||1,3):state.heroLevel||1,heroAttack=finalBattle?Math.max(state.heroAttack||8,12):state.heroAttack||8,heroMaxHp=finalBattle?Math.max(state.heroMaxHp||30,40):state.heroMaxHp||30;return{enemy,name:enemies[enemy]?.name||enemy,heroLevel,heroAttack,heroHp:heroMaxHp,heroMaxHp,enemyHp:specs.hp,enemyMaxHp:specs.hp,enemyDamage:specs.damage,guardDamage:specs.guard,bahamutDamage:specs.breath,alliesAuto:!!specs.alliesAuto,bahamutAutoDamage:18,demonKingAutoDamage:14,turn:1,potionUsed:false,bahamutUsed:false,ended:false,growthApplied:false,log:specs.alliesAuto?`${enemies[enemy]?.name||enemy}が戦闘態勢に入った。\nバハムートと魔王が援護に入る！`:`${enemies[enemy]?.name||enemy}が戦闘態勢に入った。\n行動を選んでください。`}}
export function performRpgAction(b,state,enemies,action){if(b.ended)return b;let damage=0,incoming=b.enemyDamage,message="";if(action==="attack"){damage=b.heroAttack;message=`主人公の斬撃！ ${b.name}に${damage}ダメージ。`}else if(action==="guard"){incoming=b.guardDamage;message="主人公は盾を構えた。受けるダメージを軽減！"}else if(action==="potion"&&!b.potionUsed){const healed=Math.min(10,b.heroMaxHp-b.heroHp);b.heroHp+=healed;b.potionUsed=true;message=`ポーションを使い、HPが${healed}回復した。`}else if(action==="bahamut"&&!b.bahamutUsed&&!b.alliesAuto){damage=b.bahamutDamage;b.bahamutUsed=true;state.bahamutCondition=Math.max(0,state.bahamutCondition-(enemies[b.enemy]?.bahamutConditionCost||5));state.bahamutBattleCount++;message=`バハムートの灼熱の息！ ${b.name}に${damage}ダメージ。`}else return b;if(b.alliesAuto){damage+=b.bahamutAutoDamage+b.demonKingAutoDamage;message+=`\nバハムートの追撃！ ${b.bahamutAutoDamage}ダメージ。\n魔王の暗黒魔法！ ${b.demonKingAutoDamage}ダメージ。`}b.enemyHp=Math.max(0,b.enemyHp-damage);if(b.enemyHp<=0){b.ended=true;b.result="win";b.log=`${message}\n${b.name}を倒した！`;return b}b.heroHp=Math.max(0,b.heroHp-incoming);message+=`\n${b.name}の反撃！ 主人公は${incoming}ダメージを受けた。`;if(b.heroHp<=0){b.ended=true;b.result="lose";b.log=`${message}\n主人公は力尽きた……。`;return b}b.turn++;b.log=message;return b}
export function applyVictoryGrowth(state,balance,enemy){if(!["goblin","orc"].includes(enemy))return null;state.heroBattlesWon=(state.heroBattlesWon||0)+1;state.heroLevel=(state.heroLevel||1)+1;state.heroMaxHp=(state.heroMaxHp||30)+(balance.hero?.hpGrowthPerWin||5);state.heroAttack=(state.heroAttack||8)+(balance.hero?.attackGrowthPerWin||2);return{level:state.heroLevel,maxHp:state.heroMaxHp,attack:state.heroAttack}}
